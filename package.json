{
  "name": "live-quiz-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cd frontend && npm install && npm run dev -- --host 0.0.0.0 --port 8080",
    "build": "cd frontend && npm install && npm run build",
    "preview": "cd frontend && npm run preview -- --host 0.0.0.0 --port 8080"
  }
}
